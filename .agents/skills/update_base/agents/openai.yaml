interface:
  display_name: "Update Base"
  short_description: "Refresh Sections 0-7 from the base instructions"
  default_prompt: "Use $update_base to refresh Sections 0-7 from the repo base template while preserving the current Section 8 project instructions."

policy:
  allow_implicit_invocation: false
