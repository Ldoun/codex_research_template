# Golden Examples

These examples show the canonical artifact shapes that later skills and resume behavior expect.

- `literature-only/` shows a source-heavy workflow with no experiments.
- `experiment-path/` shows a metric-bearing workflow that starts from an approved research plan and adds experiment-specific artifacts without overwriting the long-lived scope contract.

Use these as references when checking whether a skill preserved the artifact contract.
